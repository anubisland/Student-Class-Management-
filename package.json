{
  "name": "student-class-management-web",
  "version": "1.0.0",
  "private": true,
  "description": "Student Class Management System - Web App",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
