name: Scheduled Release (2-day cadence)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  scheduled-check:
    uses: anubisland/.github/.github/workflows/reusable-release-schedule.yml@main
    with:
      cadence-days: 2
    secrets: inherit
